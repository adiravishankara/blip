export const STORAGE_KEYS = {
  pendingCapture: 'blip.pendingCapture',
  settings: 'blip.settings',
} as const;

export const DEFAULT_SETTINGS = {
  selectionRequired: true,
} as const;

export const DEFAULT_WEB_APP_URL = 'http://localhost:5173';
